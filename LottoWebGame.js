var 후보군 = Array(45); // 많은 배열이 필요 할 경우에는 Array를 사용한다.
var 필 = 후보군.fill();
    [undefined, undefined, undefined];
    [1, 2, 3, 4, 5];
    // Mapping, 맵
var 맵 = 필.map(function(요소, 인덱스) {
    return 인덱스 + 1;
});

console.log(필);

var 셔플 = [];
for (var i = 0; i < 후보군.length; i += 1) {
    
}